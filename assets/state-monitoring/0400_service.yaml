apiVersion: v1
kind: Service
metadata:
  namespace: openshift-monitoring
  name: nvidia-dcgm-exporter
  labels:
    app: nvidia-dcgm-exporter
spec:
  selector:
    app: nvidia-dcgm-exporter
  type: ClusterIP
  ports:
  - name: dcgm
    port: 9101
    targetPort: 9101
    protocol: TCP